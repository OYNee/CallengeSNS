import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  margin: 0 auto;
  width: 100%;
`;

export default () =>{
  return (
    <Wrapper>
      "Notifications"
    </Wrapper>
  )
};
